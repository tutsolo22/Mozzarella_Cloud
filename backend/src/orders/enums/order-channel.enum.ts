export enum OrderChannel {
  IN_STORE = 'in_store', // Pedido en persona
  PHONE = 'phone', // Pedido por teléfono
  WHATSAPP = 'whatsapp', // Pedido por WhatsApp
  WEB = 'web', // Pedido desde la página web
}